# Carbon Nexus — Backend Dependencies
# Fixed for Python 3.10/3.11 + Windows local dev + Cloud Run compatibility

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2

# Google Cloud
google-cloud-aiplatform==1.65.0
google-cloud-firestore==2.17.2
google-cloud-pubsub==2.25.0
google-cloud-secret-manager==2.20.0
firebase-admin==6.5.0

# Vertex AI
vertexai==1.65.0

# AI Studio (free-tier Gemini key) — used in IS_LOCAL mode, also needed in
# Cloud Run since GEMINI_API_KEY is passed there too via --set-env-vars
google-generativeai==0.8.3

# LangGraph — pinned to versions compatible with each other
langgraph==0.2.28
langchain-google-vertexai==2.0.1
langchain-core==0.3.8

# tenacity — must be <9.0.0 due to langchain-core constraint
tenacity==8.5.0

# Twilio (WhatsApp delivery)
twilio==9.3.4

# Cloud Functions
functions-framework==3.8.1

# Utilities
python-dotenv==1.0.1
httpx==0.27.2